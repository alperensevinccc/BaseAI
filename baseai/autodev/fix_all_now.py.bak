"""
ERROR: Missing required config: GEMINI_API_KEY
"""

from typing import Optional, Any


def check_required_config(api_key: Optional[str]) -> None:
    """
    Check if the required configuration for GEMINI_API_KEY is provided.

    :param api_key: The GEMINI API key to check.
    :raises ValueError: If the required configuration is missing.
    """
    if not api_key:
        raise ValueError("ERROR: Missing required config: GEMINI_API_KEY")


def main(api_key: Optional[str] = None) -> None:
    """
    Main entry point for the application.

    :param api_key: The GEMINI API key.
    """
    check_required_config(api_key)
    # Additional functionality can be added here.


if __name__ == '__main__':
    import sys
    main(sys.argv[1] if len(sys.argv) > 1 else None)